# SG-011 独立复核

2026-09-20，独立只读 reviewer `/root/sg011_review`，范围限定为 SG-011 五个 storage 源文件、core exports 和三份 storage 测试。仓库无提交，按明确文件范围检查，未伪造 Git diff/提交区间。

结论：在已记录的 Windows 本地进程边界内，未确认可操作的正确性或安全缺陷。检查了路径包含关系、大小写、字节摘要、JSON 数据限制、覆盖前置条件、清理和协作并发。主代理的 50 项 storage 测试及整体回归命令证据另存于 tasks.json，reviewer 未重跑完整套件。

Reviewer 报告做过临时文件关闭错误的独立探测，并报告 Windows 原始文件符号链接探测因 EPERM 无法执行；该临时探测没有作为仓库内可复核命令日志保存，因此不计入本任务的“已验证检查”数量或阶段通过依据。

## 主代理对未判定范围的处理

- 同权限恶意文件系统竞争：沿用原稿 §16.1 的本地可绕过边界；重新检查目录不等于 OS 沙箱。若需要对抗此类攻击，后续需隔离执行及具备目录句柄能力的实现。
- Proxy 陷阱：本接口接受 JSON 解析所得普通数据，不接受可执行对象；访问器和 toJSON 已被拒绝，未宣称能无副作用审查任意 Proxy。
- Linux 的反斜杠文件名、包含 `..` 的原始符号链接目标：未在 Linux 认证。本项不宣称支持这些组合；跨平台支持前必须增加原始路径语义反例并验证，不能直接复用 Windows 通过结论。
- 断电持久性、网络文件系统、反复清理 I/O 故障：未验证，保留明确失败/残留锁可能性；不得自动清理未确认归属的文件。

无已确认缺陷被静默延期；以上范围限制保留在 SG-011 实施记录和兼容性说明中。
