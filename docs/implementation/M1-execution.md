# M1 执行记录

范围：SG-012—SG-028；SG-011 已完成。用户授权本轮完成所有 M1 任务。原稿与现有工作保持，仓库无提交，继续当前目录，不另建脚手架。

执行：每项先观察失败反例，使用 record.mjs 留存真实命令/退出码；主代理统一验收并更新 tasks.json/PROGRESS，避免并行写账本。独立子任务按文件归属隔离，依赖完成后才接通下游，不将准备工作冒充任务完成。

接口核对：Git Baseline 与 M0 BaselineIdentity 一致；InputManifest 的 UNKNOWN 对应计划文字 UNVERIFIED，保留现有严格枚举并输出具体缺口。doctor 读取 Git 身份但缺基线只报告能力缺口，绝不执行候选脚本。动态策略的路径交集不能压成 glob 字符串交集，保留每个来源的谓词约束。确认和执行信任必须分别保存并绑定不可变输入。

工具：oasdiff 从官方发布来源准备，版本启动不等于合同通过；只有真实工具合同测试完成后才更新 VERIFIED。候选 OpenAPI 安全预检在工具启动前执行。

阶段范围：只读扫描与确认/授权记录；runtime 始终 NOT_EXECUTED。M2 runner、服务启动、真实浏览器、发布不在本轮范围。
