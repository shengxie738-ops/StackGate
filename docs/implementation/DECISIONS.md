# Implementation decisions

- 2026-09-20, SG-001: preserve both root documents byte-for-byte under their planned `docs/specs` and `docs/plans` paths. The provided source SHA-256 matches the actual file; no source divergence exists.
- 2026-09-20, SG-001: task ledger uses `{ schema_version: "0.1", tasks: [...] }`. Titles, phases and direct dependencies come from the original plan index; validation rejects changes to those fields.
- 2026-09-20, SG-001: bootstrap verification uses Node's built-in test runner before package installation. The evidence runner preserves actual child-process exit codes and runs supported Windows package managers through their resolved JavaScript entry, with no implicit shell fallback. It records local development evidence, not product Run evidence.
- 2026-09-20, SG-001: no local commit has been created by this task; task commit remains null.
- 2026-09-20, SG-002—010: implementation baseline and explicit limitations are recorded in `docs/adr/ADR-010-implementation-baseline.md` and `docs/COMPATIBILITY.md`. All versions come from actual local tools, with official source URLs and integrity in `tools/compatibility-lock.json`.
- 2026-09-20, SG-010: M0 adds lint and real toolchain contracts to the seven planned minimum checks. Unimplemented later-stage entries remain nonzero. No browser, container, service, packaging or CI result is inferred from these checks.
- 2026-09-20, SG-011: retain the task-card's two-argument atomic-write interface and add optional explicit root/expectedHash preconditions; new files never clobber, replacements require caller-confirmed ownership and matching prior bytes. Storage I/O is outside the pure domain. Full rulings, remaining boundaries and independent review are in `docs/implementation/SG-011.md` and `reviews/SG-011.md`.
- 2026-09-20, M1: repository identities use schema-compatible `repo_<sha256>` / `worktree_<sha256>` IDs. Real Git manifests are validated against the existing schema; the schema was not relaxed to accept invalid identifiers. Hashes remain bare lowercase SHA-256 in shared schemas; OpenAPI loader raw source hashes carry an explicit `sha256:` prefix at its adapter boundary.
- 2026-09-20, SG-017/026: immutable task revision snapshots seal original check contracts, protected-file hashes, configuration and repository identities. Preview fields are re-derived and verified. Static scan compares current acceptance inputs, reporting DENY/review gaps separately from contract compatibility. No runtime inventory is invented.
- 2026-09-20, SG-016/025: path permissions preserve an AND of layer-level glob predicates; required checks only grow. Workspace fallback sets must be supplied from confirmed policy. The CLI does not infer a trusted fallback set from static candidates; absent declared authority remains a coverage gap.
- 2026-09-20, SG-018/020: reviewed application analysis tools use pinned deployment identities; business execution grants are separate, stored outside the repository and bound to the complete execution preview. Expired matching grants renew using an exact previous-byte CAS; external script arguments are rejected until covered by a supported input scope.
- 2026-09-20, SG-027: the public scan DTO replaces temporary source locations with relative labels and emits raw-output digests; adapter results and development contract evidence retain the unmodified tool records. Initial presets create configuration and a DRAFT task only; runnable FastAPI/React templates belong to SG-079.
- 2026-09-20, SG-028: M1 includes every M0 mandatory check plus the full integration suite and verified real oasdiff requirement. Requested stage identity must match its manifest. Historical RED/setup failures remain in the ledger; final stage output is the completion authority.
